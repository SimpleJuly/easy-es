<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.dromara.easy-es</groupId>
        <artifactId>easy-es-parent</artifactId>
        <version>3.3.0</version>
        <relativePath>../easy-es-parent</relativePath>
    </parent>

    <artifactId>easy-es-boot4-starter</artifactId>
    <name>easy-es-boot4-starter</name>
    <description>Easy-ES starter for Spring Boot 4.x</description>

    <dependencies>
        <dependency>
            <groupId>org.dromara.easy-es</groupId>
            <artifactId>easy-es-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.dromara.easy-es</groupId>
            <artifactId>easy-es-spring</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>
    </dependencies>

    <build>
        <!-- Boot 3/4 当前共用自动配置 API；共享源码，依赖链分别选择 ES8/ES9。 -->
        <sourceDirectory>${project.basedir}/../easy-es-boot-starter/src/main/java</sourceDirectory>
        <resources>
            <resource>
                <directory>${project.basedir}/../easy-es-boot-starter/src/main/resources</directory>
            </resource>
        </resources>
    </build>
</project>
